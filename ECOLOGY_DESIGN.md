# 生态关系网解谜：可解性证明与裁决设计

## 1. 可解性怎么证明

每关不是“把标准答案藏起来”，而是一个带生态谓词的约束满足问题（CSP）：

1. 作者只提供已验证的营养关系模板和一个构造性见证（canonical witness）。
2. 服务端生成时对空位做双射重命名、重排牌堆和边编号；几何、标签和牌堆顺序不进入约束。
3. 用与玩家裁决完全相同的谓词检查每条候选边：
   - `input`：非生物源必须生产目标使用的外部底物；
   - `syntrophy`：上游必须生产、下游必须消费同一代谢物；
   - `symbiosis`：交换资源或宿主—共生体识别标记必须匹配；
   - `predation`：猎物标签必须在捕食者食性内；
   - `competition`：双方必须共享限制性生态位。
4. 独立的确定性回溯求解器枚举候选放置；每个候选都重放完整裁决，而不是信任作者见证。
5. 找到至少一个完整解后，生成才成功，否则服务端抛错，不会发题。
6. 生成期计算约束哈希。洗牌前后哈希必须相同，用来证明只改变了公开标识和呈现，没有改变 CSP。

见证只保存在服务端 session 与本地验收 artifact；公开接口只返回哈希、求解器结论和 soundness checks。

## 2. 网络为什么会“塌”

激活过程是碳源传播的最小不动点：

- 非生物环境初始激活；
- 生物节点至少有一条合法、且来源已激活的营养/共生支持；
- 它声明的限制资源也必须已由激活上游提供；
- 迭代直到没有新节点可激活。

因此：

- 没有合法入边：`no-carbon-source`；
- 未激活节点形成相互等待的支持环：`deadlock-cycle`，Tarjan SCC 标出环；
- 入边合法但上游饿死或死锁：`upstream-collapse`。

## 3. 多解策略

关卡不与固定 DOM 或固定空位 ID 绑定。服务端回溯求解器验证：**所有满足完整生态关系与激活不动点的无重复分配都通过**。返回 `solver.solutionCount`；达到枚举上限时返回 `"many"`。

## 4. 确定性与防篡改

- 谜题、答案、边和锁定节点只来自服务端 session。
- 客户端提交 `slotId -> speciesId`，服务端重新校验形状、关系和碳流；改 DOM、改分数或只读本地状态都不能影响裁决。
- 决策输入为规则版本、关卡、按 slotId 排序的分配和结论，经 SHA-256 生成 `decisionHash`。
- 同一 session 中同一份提交重复提交，结论、分数和哈希必须完全一致。

## 5. 分级提示

- 卡住 120 秒：一级“功能角色”提示，扣 5 分；
- 卡住 180 秒：二级“相邻关系方向”提示，再扣 10 分；
- 卡住 240 秒：三级“候选范围收窄”提示，再扣 20 分。

提示只描述生态角色、相邻关系和候选数量，不返回物种 ID 或完整答案；扣分只在最终通过时结算。

## 6. 输入边界

服务端拒绝：未知空位、改写锁定节点、未知物种、重复物种、跨生态系统物种、缺失空位、未知关系类型、非法边。非法关系和未激活节点会同时返回给前端用于可视化。

## 7. 前端交互与布局

- Pointer Events 统一支持鼠标、触摸和触控笔；键盘支持 Tab、Enter/Space、方向键、Delete/Escape。
- SVG 自己绘制箭头、节点和状态；本项目关卡节点使用服务端给定几何，通用布局器用于动态/大图。
- `shared/graphLayout.ts` 的确定性力导向 + 碰撞松弛在验收中实测 100 节点、160 边中位耗时远低于 200ms，且最终重叠数为 0。

验收命令：

```bash
npm run acceptance
```

产物位于 `acceptance-artifacts/`：

- `solvability-proofs.json`：每关至少一个解的生成期证明；
- `deterministic-grade.json`：同一提交两次结论一致的断言数据；
- `layout-100-nodes.json`：100 节点布局实测耗时。
